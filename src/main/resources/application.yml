spring:
  application:
    name: medconnect
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
  jpa:
    properties:
      hibernate:
        default_schema: medconnect
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show-sql: true
        ddl-auto: none
    open-in-view: false
  flyway:
    enabled: true
    baseline-on-migrate: true
    validate-on-migrate: false
    table: medconnect_schema_version
    default-schema: medconnect
    baseline-version: 0
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: "3f2c1f8b879159"
    password: "d311b77d4225bc"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

vars:
  security:
    enable: true

server:
  port: 8090

token:
  ttl: 3600000
  secret: "AceastaEsteOCheieSecretaExtremDeLungaSiComplexaPentruProiectulMedConnect2026_TrebuieSaAibaPeste64Caractere"

admin:
  username: admin
  email: admin@admin.com
  password: admin